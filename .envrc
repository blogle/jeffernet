dotenv
export TF_VAR_cloudflare_account_id="${CLOUDFLARE_ACCOUNT_ID:?CLOUDFLARE_ACCOUNT_ID must be set in .env}"
if [[ -n "${GOOGLE_SITE_VERIFICATION_TOKEN:-}" ]]; then
  export TF_VAR_google_site_verification_token="$GOOGLE_SITE_VERIFICATION_TOKEN"
fi
